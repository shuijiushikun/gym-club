<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gym.club.mapper.CoachMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO coach (username, password, real_name, phone, email, gender, birthday, 
                          avatar_url, certificate, specialty, experience_years, hourly_rate, status)
        VALUES (#{username}, #{password}, #{realName}, #{phone}, #{email}, #{gender}, #{birthday},
                #{avatarUrl}, #{certificate}, #{specialty}, #{experienceYears}, #{hourlyRate}, #{status})
    </insert>

    <select id="selectById" resultType="com.gym.club.entity.Coach">
        SELECT * FROM coach WHERE id = #{id}
    </select>

    <select id="selectByUsername" resultType="com.gym.club.entity.Coach">
        SELECT * FROM coach WHERE username = #{username}
    </select>

    <select id="selectAll" resultType="com.gym.club.entity.Coach">
        SELECT * FROM coach ORDER BY id DESC
    </select>

    <update id="update">
        UPDATE coach
        <set>
            <if test="username != null">username = #{username},</if>
            <if test="password != null">password = #{password},</if>
            <if test="realName != null">real_name = #{realName},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="email != null">email = #{email},</if>
            <if test="gender != null">gender = #{gender},</if>
            <if test="birthday != null">birthday = #{birthday},</if>
            <if test="avatarUrl != null">avatar_url = #{avatarUrl},</if>
            <if test="certificate != null">certificate = #{certificate},</if>
            <if test="specialty != null">specialty = #{specialty},</if>
            <if test="experienceYears != null">experience_years = #{experienceYears},</if>
            <if test="hourlyRate != null">hourly_rate = #{hourlyRate},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM coach WHERE id = #{id}
    </delete>
</mapper>